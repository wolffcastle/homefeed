package coding.challenge.homefeed.domain;

/**
 * 
 * HomefeedModule internal domain interface
 * Defines the common structure for all homefeed modules.
 * Independent of the transport layer (HTTP/REST/API-Version), this interface is used to define the structure of homefeed modules in the domain layer.
 */
public interface HomefeedModule {
    
}
